{
    "scripts": {
        "start": "serve -s front-end/build",
        "heroku-postbuild": "cd front-end && npm install && npm run build"
    },
    "dependencies": {
        "serve": "^12.0.0"
    }
}
